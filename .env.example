# ── Luzar · variables de entorno (documentadas) ──
# Copia este archivo a .env.local y rellena los valores. NUNCA subas .env.local.

# Endpoint de envío del formulario de contacto (API route / servicio externo)
CONTACT_FORM_ENDPOINT=

# Número de WhatsApp para el botón flotante (formato internacional, sin +)
NEXT_PUBLIC_WHATSAPP_NUMBER=528110660710
